[pytest]
# test_micro.py est un script de DIAGNOSTIC micro (lancé à la main), pas une suite
# pytest : il ouvre un device audio à l'import et casse la collection. On l'exclut
# pour que `pytest` ne ramasse que les vraies suites (test_segmentation.py).
addopts = --ignore=test_micro.py
